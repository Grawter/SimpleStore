@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewBag.Title = "Список ролей";
}

<h3>Список ролей</h3>
<table class="table table-hover col-md-4">
    @foreach (var role in Model)
    {
        <tr>
            <td>
                @role.Name
            </td>
            <td>
                <a asp-action="DeleteRole" asp-route-id="@role.Id" class="btn btn-sm btn-outline-danger">Удалить</a>
            </td>
        </tr>
    }
</table>
<a class="btn btn-outline-primary" asp-action="CreateRole">Добавить роль</a>
<a class="btn btn-outline-primary" asp-action="Index">Список пользователей</a>