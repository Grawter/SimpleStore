@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewBag.Title = "Список ролей";
}

<h3>Список ролей</h3>
<table class="table">
    @foreach (var role in Model)
    {
        <tr>
            <td>
                @role.Name
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@role.Id" class="btn btn-sm btn-outline-danger">Удалить</a>
            </td>
        </tr>
    }
</table>
<a class="btn btn-outline-primary" asp-action="Create">Добавить роль</a>
<a class="btn btn-outline-primary" asp-action="UserList">Список пользователей</a>